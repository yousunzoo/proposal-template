# DESIGN.md — 제안서 템플릿

> **라이트 리딩 테마** — 장문 제안서 가독성 우선. 선명한 블루 액센트(#1B98FF)를 절제해 사용.
> 제안서·포트폴리오 상세 모두 라이트로 통일.

## 1. 디자인 원칙

1. **가독성 최우선** — 흰 문서 서페이스 + 슬레이트 고대비 텍스트. 장문을 편하게 읽는다.
2. **절제된 강조** — 블루는 헤드라인 하이라이트·지표·액티브 상태에만.
3. **소프트 섀도 카드** — 흰 카드 + 은은한 그림자로 위계, 과한 테두리 지양.
4. **일관된 리듬** — eyebrow → 헤드라인 → 본문. base(흰색) ↔ elevated(옅은 블루틴트) 밴드 교차.
5. **모션 최소** — 스크롤 진입 애니메이션 없음. 사용자 조작 스크롤만 스무스.

## 2. 색 (라이트 토큰)

| 토큰 | 값 | 용도 |
|------|-----|------|
| `canvas` / `surface` | `#ffffff` | 페이지·카드 |
| `elevated` | `#f5f8fd` | 교차 밴드·내부 요소 |
| `line` / `line-strong` | `#e6ebf2` / `#d3dbe6` | 보더 |
| `blue-500` | `#1b98ff` | 채움 요소(팀 pill, 간트 바), 버튼 hover |
| `blue-600` | `#007ee5` | 버튼, 텍스트 포인트·강조 |
| `blue-700` | `#0065b8` | 링크·배지 텍스트 |
| `ink-900` | `#0f172a` | 제목 |
| `ink-600` | `#475569` | 본문 |
| `ink-500` | `#64748b` | 캡션·메타 |
| 상태 | green `#16a34a` · amber `#d97706` · red `#dc2626` | 성공/경고/에러 |

- 배지(Chip): `bg-blue-50 + border-blue-200 + text-blue-700`.
- 헤드라인 그라데이션 `text-gradient-hero`: `#0f172a → #0a6fd1` (ink→blue, 라이트 대비).

## 3. 타이포그래피

- Font: **Pretendard Variable**. 본문 base 16px / line-height 1.6~1.85.
- display 60 / h1 50 / h2 36 / title 20~28 / body 15~18 / detail 12. tracking ≈ -2%.
- 헤드라인은 gradient-hero 또는 ink-900, 강조 단어만 blue-600.

## 4. 컴포넌트

- **Card**: 흰 배경 + `border-line` + 소프트 섀도(`0 10px 28px -16px rgba(15,23,42,.14)`), hover 섀도 강화.
- **Eyebrow**: 번호(blue-600) + 라인 + 대문자 라벨(tracking 0.2em, ink-500).
- **Button(primary)**: `bg-blue-600` 흰 텍스트 + 소프트 섀도, hover `blue-500`.
- **Metric / 간트 바**: gradient-hero 숫자 / `bg-blue-500` 바.

## 5. 레이아웃

- 콘텐츠 최대폭 `1120px`(본문 리딩 720~860px). 좌우 20/32px.
- 공개 제안서(`/p/[slug]`): 좌측 고정 라이트 사이드바(스크롤 스파이, 액티브 `bg-blue-50`) + 섹션 스택.
- 섹션 세로 간격 80~112px. 마무리(성공을 향한 약속)는 블루틴트 밴드.
- **포트폴리오 상세**: 익명 케이스스터디를 라이트 리딩 톤으로 구성(케이스스터디 밴드 + 라이트 CTA/관련).

## 10. Voice (AI 티 제거)

- 사실 기반 단정문. 금지: "혁신적/최첨단/원활한/seamless/손쉽게", 이모지 남발.
- 발주 플랫폼 제약상 **회사명·연락처 등 신원 정보 노출 금지**.
